export {};

type ProfileType = { name: string };
let typeObject: ProfileType = {
  name: 'name',
};

interface ProfileInterface {
  name: string;
}
let interfaceObject: ProfileInterface = {
  name: 'name',
};
